type Metadata struct {
	# Previous metadata.
	PreviousID nullable Link_Metadata
    # Provider ID of the metadata
    Provider String
    # Cache switch
    Cache optional Bool
    # Collection name of the metadata
    Collection optional String
	# data
	Payload   Any
	# metadata signature.
    Signature Bytes
}

type Link_Metadata &Metadata
